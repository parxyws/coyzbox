template: testify
packages:
  github.com/parxyws/cozybox/internal/app/auth:
    config:
      dir: internal/app/auth/mocks
      filename: "mock_{{.InterfaceName | snakecase}}.go"
    interfaces:
      UserRepository:
      TenantRepository:
      TenantMemberRepository:
      OrganizationRepository:
      SessionStore:
      Mailer:
      FileStorage:
      AuthService:
      TransactionManager:
      RepoFactory:
  github.com/parxyws/cozybox/internal/app/user:
    config:
      dir: internal/app/user/mocks
      filename: "mock_{{.InterfaceName | snakecase}}.go"
    interfaces:
      UserRepository:
      TenantRepository:
      TenantMemberRepository:
      OrganizationRepository:
